import 'package:flutter/material.dart';

class Enseignant extends StatelessWidget {
  @override
  Enseignant({Key key}) : super(key: key);

  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('hello'),
    );
  }
}
